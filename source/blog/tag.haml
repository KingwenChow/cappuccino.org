---
pageable: true
per_page: 10
layout: blog
---
- @title = "Blog - #{tagname}"

%h1.header
  Articles tagged '#{tagname}'

- page_articles.each do |current_article|
  = partial "partials/blog_article_list_format", :locals => { :current_article => current_article }

= partial "partials/blog_pagination", :locals => { :prev_page => prev_page, :page_number => page_number, :num_pages => num_pages, :next_page => next_page }
